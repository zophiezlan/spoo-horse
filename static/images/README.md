# Image Naming Convention

It is recommended to use a consistent prefixing convention for image files to differentiate their types. For example:

* `bg-` for background images
* `icon-` for icons
* `hero-` or `banner-` for hero banners
